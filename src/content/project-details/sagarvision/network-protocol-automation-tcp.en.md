I connected TCP/IP communication between Unity clients and external devices, then organized protocol handling so repeated packet parsing and message flow could be maintained more predictably.

- Integrated TCP/IP socket communication into the Unity client.
- Structured protocol handling to reduce repetitive parsing logic.
- Separated communication flow from presentation-facing Unity components.
